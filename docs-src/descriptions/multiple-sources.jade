h2#combine-properties Combine properties



+descr-method('combine', 'combine', 'Kefir.combine(properties, [fn])').


+descr-method('sampled-by', 'sampledBy', 'Kefir.sampledBy(properties, samplers, [fn])').


+descr-method('merge', 'merge', 'Kefir.merge(properties)').


+descr-method('pool', 'pool', 'Kefir.pool()').


+descr-method('flat-map', 'flatMap', 'property.flatMap(fn)').


+descr-method('flat-map-latest', 'flatMapLatest', 'property.flatMapLatest(fn)').


