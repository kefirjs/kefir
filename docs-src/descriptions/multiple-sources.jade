h2#combine-observables Combine observables



+descr-method('combine', 'combine', 'Kefir.combine(observables, [fn])').


+descr-method('sampled-by', 'sampledBy', 'Kefir.sampledBy(passiveObservables, activeObservables, [fn])').


+descr-method('merge', 'merge', 'Kefir.merge(observables)').


+descr-method('pool', 'pool', 'Kefir.pool()').


+descr-method('flat-map', 'flatMap', 'property.flatMap(fn)').


//- +descr-method('flat-map-latest', 'flatMapLatest', 'property.flatMapLatest(fn)').


