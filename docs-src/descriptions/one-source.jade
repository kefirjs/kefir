h2#modify Modify an observable


+descr-method('map', 'map', 'observable.map(fn)').


+descr-method('filter', 'filter', 'observable.filter(fn)').


+descr-method('take', 'take', 'observable.take(n)').


+descr-method('take-while', 'takeWhile', 'observable.takeWhile(fn)').


+descr-method('skip', 'skip', 'observable.skip(n)').


+descr-method('skip-while', 'skipWhile', 'observable.skipWhile(fn)').


+descr-method('skip-duplicates', 'skipDuplicates', 'observable.skipDuplicates([fn])').


+descr-method('diff', 'diff', 'observable.diff(seed, fn)').


+descr-method('scan', 'scan', 'observable.scan(seed, fn)').


+descr-method('reduce', 'reduce', 'observable.reduce(seed, fn)').


+descr-method('with-handler', 'withHandler', 'observable.withHandler(fn)').


+descr-method('delay', 'delay', 'observable.delay(wait)').


+descr-method('throttle', 'throttle', 'observable.throttle(wait, [options])').


